var Source = require "./source";
var Lexer = require "./lexer";

export create(text, file):
	return new Source(text, file);

export lexer(text, file, sugar_box):
	return Lexer(text, file, sugar_box);


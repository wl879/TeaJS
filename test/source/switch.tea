// #: if / while / do / break / continue 语句
switch de:
	case 'a','b','c': default:
		block5;

switch de:
	case 'a','b','c':
	case 'd':
		continue;
	case ( a < 3):
		block3;
	case (a > 10):
		block4;
	default:
		block5;
